# 交接日志 2026-07-26

## 今天完成的（已验证、已提交、可以直接使用）

### 1. 蛛网降噪
- collectors/06_file_tree.sh、03_recent_files.sh、07_todo_grep.sh
  三个 collector 都加了 `_archive|dist|__pycache__|node_modules` 排除
- .gitignore 补充 `*.bak`、`state/*.last_snapshot`
- 全部已提交，快照命令(lu/sms/dj/kk/wang)输出应该都是干净的

### 2. 炉的操作日志
- 新文件 ~/lu/core/op_log.py：log_op(action, target, result, detail) 追加写入 ~/lu/logs/operations.jsonl，只增不改，带 cwd 字段
- lu_patch.py 的 do_replace / do_rollback 已接入：成功、失败（包括规则拦截、原子写失败、找不到快照）全部记录
- 新文件 ~/lu/core/view_log.py + .bashrc 里新增函数 `rz`（跟蛛网的项目别名机制无关，独立函数）
  - `rz` 看最近20条，所有项目共用一份日志
  - `rz 关键词` 按 cwd 或 target 过滤，比如 `rz sms`
  - logs/ 已加入 .gitignore，不进 git

### 3. 定界接入炉的日志
- ~/sms/invariants/registry_contracts.py 的 check_upgrade_preconditions
  现在四个失败分支 + 一个成功分支都调用 op_log.log_op("contract_check", ...)
  用 sys.path 插入 ~/lu/core 来 import，套了 try/except，op_log 不在场也不会让契约检查本身崩掉
- 已用 verify_dj_log.py 验证过：正常升级/版本回退两个场景都正确记录进同一份 rz 视图

**验证方式**：任意目录下敲 `rz` 就能看到炉的写入记录和定界的契约检查记录混在一条时间线里。

## 还没做的（按你之前列的清单，原样列出，没有过度加工）

优先级仅供参考，不是必须按这个顺序：

1. **sms 结构性清理**（体量最大，建议单独开一轮，别和别的任务混）
   - 111个文件只有15个在用，build/、pipeline/、planner/ 三套子系统需决策保留还是归档
   - core/module.py 和 module/module.py 新旧定义并存的僵尸代码需清理
2. **state/quality_state 双字段合并**
   - 现状：两个字段独立存在，promote() 会同步更新，但构造函数可以绕过，定界目前的前置条件检查拦不住这种设计缺陷
   - 需要先决定合并方案（哪个留哪个删），再让 promote() 变成唯一入口
3. **定界补充不变量检查**（不只是前置条件）
   - 目标：能在构造期发现 state/quality_state 不同步这类问题，而不是只在调用时拦
4. **定界注册期检查**（和 sms 的 import 提取结合）
   - 目标：模块间契约在注册时就检查，不用等到真正调用才暴露问题
5. **炉的锚点匹配脆弱**
   - 今天的操作里，凡是遇到"整段替换"就有概率因为空格/换行细节不通过唯一性校验，靠人工缩短锚点绕过的，没有从工具层解决
   - 潜在方向：支持"行号范围替换"模式跳过全文唯一性匹配；或"整文件替换"模式
6. **炉的操作过程仍不够轻量**
   - 改一行代码还是要走"构造old文件→构造new文件→执行patch"三步，今天全程都是这样操作的
7. **蛛网注册新项目不够自动**
   - 目前还是要手动 `zw add` 或改 projects.conf

## 新会话怎么用这份交接

把这份文件内容贴给新窗口的AI（不需要贴今天原始的问答过程），再贴一下你要处理的具体项目的 `sms`/`lu`/`dj` 等快照命令输出，就能直接从"还没做的"清单里选一项继续，不用重新解释背景。
